profiles:
  base:
    ignore_name:
      - "*.bak"
      - "*.o"
      - "*.old"
      - "*~"
      - ".git"

  data:
    include: base
    white_list:
      - "folder/foo~/toto.bak"
    ignore_path:
      - "*/bar"
